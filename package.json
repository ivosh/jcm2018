{
  "name": "jcm2018",
  "version": "0.1.0",
  "description": "Jirkovský crossmarathon",
  "keywords": [],
  "author": "Ivo Raisr",
  "email": "ivosh@ivosh.net",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ivosh/jcm2018.git"
  },
  "engines": {
    "node": "8.8.x"
  },
  "cacheDirectories": [
    "node_modules",
    "ui/node_modules"
  ],
  "scripts": {
    "start": "node server",
    "test": "npm run test:server && cd ui/ && npm test && cd ..",
    "test:server": "NODE_ENV=test LOG_LEVEL=error jest --env=node server --",
    "prettier": "prettier --write 'server/**/*.js' && cd ui/ && npm run prettier && cd ..",
    "coverage": "NODE_ENV=test LOG_LEVEL=error jest --env=node --coverage server && cd ui/ && npm run coverage && cd ..",
    "coverage:report": "npm run coverage && codecov && cd ui/ && codecov && cd ..",
    "clean": "rm -rf coverage && cd ui/ && npm run clean && cd ..",
    "clobber": "rm -rf coverage node_modules && cd ui/ && npm run clobber && cd ..",
    "heroku-postbuild": "cd ui/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build && cd .."
  },
  "dependencies": {
    "async-file": "^2.0.2",
    "heroku-logger": "^0.3.0",
    "node": "^8.9.0",
    "websocket": "^1.0.25"
  },
  "devDependencies": {
    "codecov": "^3.0.0",
    "jest": "^21.2.1",
    "prettier": "^1.7.4",
    "supertest": "^3.0.0"
  },
  "jest": {
    "coverageDirectory": "./coverage/"
  }
}
