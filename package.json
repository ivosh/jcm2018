{
  "name": "jcm2018",
  "version": "0.1.0",
  "description": "Jirkovský crossmarathon",
  "keywords": [],
  "author": "Ivo Raisr",
  "email": "ivosh@ivosh.net",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ivosh/jcm2018.git"
  },
  "engines": {
    "node": "8.8.x"
  },
  "cacheDirectories": [
    "node_modules",
    "ui/node_modules"
  ],
  "scripts": {
    "start": "node server",
    "test": "cd ui && npm test",
    "prettier": "prettier --write '{server,__{tests,mocks}__}/**/*.js' && cd ui/ && npm run prettier",
    "clean": "rm -rf coverage && cd ui/ && npm run clean",
    "clobber": "rm -rf coverage node_modules && cd ui/ && npm run clobber",
    "heroku-postbuild": "cd ui/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "dependencies": {
    "async-file": "^2.0.2",
    "node": "^8.8.1",
    "websocket": "^1.0.25"
  },
  "devDependencies": {
    "prettier": "^1.7.4"
  }
}
